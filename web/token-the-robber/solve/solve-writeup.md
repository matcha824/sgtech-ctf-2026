# Overview
This challenge primarily relies  on an understanding of JWT tokens, how they are decoded/encoded, and how sites  use them.

# Finding the guest credentials
You can find the guest credentials (which give you access to the JWT you'll need to  decode). 
