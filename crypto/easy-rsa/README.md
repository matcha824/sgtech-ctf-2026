# Name
Easy RSA

# Flag
Contained in `solve/flag.txt`

# Message
Rivest-Shamir-Adleman (RSA) is one of the oldest public-key cryptosystems out there. It relies on the fact that currently, factoring the product of two large prime numbers is very difficult. Can you break it?

# Files to include with the challenge
`chall-file.txt`
`sample-script.py`

# Hints
- Not all RSA moduli are created equal.
