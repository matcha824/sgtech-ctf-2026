# Notes

We first made an encrypted message using AEC CBC.

The file secret-image.png was used by Gemini to generate an image of the concept of "hiding in plain sight". The image features some clues visually as well as in the image metadata to help solve the challenge.

A python script (`embed-key.py`) was used to embed the secret key after base85 encoding.